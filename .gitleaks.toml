# Gitleaks allowlist — Airtable record ID remaps are not secrets
title = "Dealality gitleaks"

[allowlist]
description = "Hotel census Legacy→HPC id remap (Airtable rec ids)"
paths = [
  '''config/hotel-census/legacy-to-hpc-id-map\.json''',
  '''data/hotel-census-v2/.*/legacy-to-hpc-id-map\.json''',
]
